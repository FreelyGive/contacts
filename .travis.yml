# @file
# .travis.yml - Drupal 8 Travis CI Integration

language: php

sudo: false

php:
  - 5.6
  - 7

# Set up environment variables.
env:
  global:
    # add composer's global bin directory to the path
    # see: https://github.com/drush-ops/drush#install---composer
    - PATH="$PATH:$HOME/.composer/vendor/bin"

    # Configuration variables.
    - DRUPAL_TI_MODULE_NAME="contacts"
    #- DRUPAL_TI_SIMPLETEST_GROUP="user"
    - DRUPAL_TI_PULL_SIMPLETEST_GROUP="contacts"
    - DRUPAL_TI_NON_PULL_SIMPLETEST_GROUP="user,contacts"
    #- DRUPAL_TI_NON_PULL_SIMPLETEST_GROUP="--all"

    # The environment to use, supported are: drupal-7, drupal-8
    - DRUPAL_TI_ENVIRONMENT="drupal-8"

    # Drupal specific variables.
    - DRUPAL_TI_DB="drupal_travis_db"
    - DRUPAL_TI_DB_URL="mysql://root:@127.0.0.1/drupal_travis_db"
    # Note: Do not add a trailing slash here.
    - DRUPAL_TI_WEBSERVER_URL="http://127.0.0.1"
    - DRUPAL_TI_WEBSERVER_PORT="8080"

    # Simpletest specific commandline arguments, the DRUPAL_TI_SIMPLETEST_GROUP is appended at the end.
    - DRUPAL_TI_SIMPLETEST_ARGS="--verbose --color --concurrency 4 --url $DRUPAL_TI_WEBSERVER_URL:$DRUPAL_TI_WEBSERVER_PORT"
    - SIMPLETEST_BASE_URL="http://127.0.0.1:8080"

  matrix:
    #- We run phpunit manually in ./travis-ci/travis-script.sh
    - DRUPAL_TI_RUNNERS="simpletest"

mysql:
  database: drupal_travis_db
  username: root
  encoding: utf8

before_install:
  - composer self-update
  - git config --global github.accesstoken $GITHUB_OAUTH_TOKEN
  - composer global require "lionsad/drupal_ti:dev-master"
  - drupal-ti before_install

install:
  - drupal-ti install

before_script:
  - drupal-ti --include "./travis-ci/travis-before-script.sh"
  - drupal-ti before_script

script:
  - drupal-ti --include "./travis-ci/travis-script.sh"
  - drupal-ti script

after_script:
  - drupal-ti after_script

notifications:
  #email: yan@yanniboi.com
  irc: "chat.freenode.net#freelygive"
